import { ParticipationPipe } from './participation.pipe';

describe('ParticipationPipe', () => {
  it('create an instance', () => {
    const pipe = new ParticipationPipe();
    expect(pipe).toBeTruthy();
  });
});
